{
  "name": "production-mev-bot",
  "version": "3.0.0",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "deploy": "hardhat run scripts/deploy.ts --network mainnet"
  },
  "dependencies": {
    "ethers": "6.7.1", 
    "@flashbots/ethers-provider-bundle": "^1.0.0", 
    "winston": "^3.11.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "typescript": "latest",
    "@types/node": "latest",
    "hardhat": "latest",
    // Pinning the toolbox and related dependency to a version 
    // that likely pre-dates the switch to the ^6.14.0 requirement.
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    // We explicitly include this dev dependency to ensure it gets the older version
    "@nomicfoundation/hardhat-ethers": "3.0.0" 
  }
}
